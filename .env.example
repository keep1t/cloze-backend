# Used only by local tooling or deployment commands. Never commit real values.
SUPABASE_PROJECT_REF=
SUPABASE_ACCESS_TOKEN=

# Server-only secret consumed by the future outfit recommendation Edge Function.
GEMINI_API_KEY=
